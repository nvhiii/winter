let ourPlanet = "Earth"; // can be const bc our planet never changes

let currentVisitor;

// if we know a variable value before runtime, meaning no function calls needed, we can make that cosnt variable uppercase
// on the contrary, if we do not, then keep it lowercase